# 둘 중 한 방식으로 설정하세요.

# 1) 연결 문자열 한 줄로
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/memo_app

# 2) 개별 항목으로 (DATABASE_URL 이 없을 때 사용됨)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=memo_app

# 웹 서버 포트
PORT=3000
