# Supabase / PostgreSQL 연결 문자열 (한 줄)
# Supabase 대시보드 → Project Settings → Database → Connection string → "Connection pooling"(포트 6543) 권장
DATABASE_URL=postgresql://USER:PASSWORD@HOST:6543/postgres

# 웹 서버 포트
PORT=3000
